# Used automatically by `npm run build` (Vite production mode).
# Local dev (`npm run dev`) still uses .env (localhost). Keep these separate so
# production builds never ship the localhost API URL.
VITE_BASE_URL = https://tamirijewellers.com:2031/website_api
